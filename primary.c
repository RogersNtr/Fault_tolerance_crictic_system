#include<stdio.h>
#include<stdlib.h>
#include<pthread.h>
#include<unistd.h>


// Calculate the
float mean_calculation(int **sensor){

   /*  while(1){
        printf("thread fonction");
        sleep(10);
    }  */
/*     int* nb_iteration = (int*) malloc(sizeof(int));
    * nb_iteration = rand()%6;
    for(int i=0;i<*nb_iteration;i++){
        printf("thread fonction %lu\n", (long)pthread_self());
        sleep(1);
  }
  return nb_iteration; */
}
